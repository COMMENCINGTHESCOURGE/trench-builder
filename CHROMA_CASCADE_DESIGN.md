# CHROMA CASCADE — Master Design Document
# Gemini-designed. To be implemented in TRENCH BUILDER.
# May 16, 2026

# ═══════════════════════════════════════════════════════
# CHECKPOINT MAPPING: 9 Layers = 9 Stages (IR + UV added)
# ═══════════════════════════════════════════════════════

CHROMA_CHECKPOINTS = """
  0. INFRARED (Thermal)  = PRE-SUPINE  Heat before movement. Potential energy.
  1. RED (Ruby)          = SUPINE      Dense gravity, heavy foundations
  2. ORANGE (Tangerine)  = SCOOT       Thermal updrafts, wind power
  3. YELLOW (Gold)       = CRAWL       Photon saturation, light bending
  4. GREEN (Viridian)    = STAND       Bio-responsive, organic growth
  5. BLUE (Cobalt)       = BOUNCE      Zero-G, momentum-based movement
  6. INDIGO (Void)       = WALK        Reverse gravity, sideways building
  7. VIOLET (Singularity)= JUMP        Time dilation, first flight
  8. ULTRAVIOLET (Spectral)= RUN       Beyond visible. Pure energy. Sustained.
"""

# Infrared = the heat you feel before you move. The preparation.
# Ultraviolet = what you can't see but you know is there. The transcendence.
# 7 visible + 2 invisible = 9 total. The full spectrum.

# ═══════════════════════════════════════════════════════
# VINCULUM MAPPING: Each layer IS a fraction
# ═══════════════════════════════════════════════════════

VINCULUM_LAYERS = """
  Layer N physics / Layer N+1 physics = transition zone
  Red/Orange = gravity gradient (2G → 0.5G over 100m)
  Yellow/Green = light absorption gradient
  Blue/Indigo = gravity reversal boundary
  Indigo/Violet = time dilation onset
  
  The Prism Storm = superposition of all fractions simultaneously
  All 7 layers / 1 reality = CHAOS for 30 seconds
"""

# ═══════════════════════════════════════════════════════
# BUILDING VERBS → CHECKPOINT SYSTEM
# ═══════════════════════════════════════════════════════

BUILDING_TO_CHECKPOINT = """
  EXTRUDE = GROW    (add new poly from face)     = SCOOT → CRAWL
  BEVEL   = REFINE  (sharpen or round edge)       = STAND → BOUNCE
  WELD    = CONNECT (merge two structures)         = CRAWL → STAND
  PAINT   = TRANSFORM (change atmospheric affinity)= JUMP → RUN
"""

# ═══════════════════════════════════════════════════════
# EXISTING TRENCH BUILDER CONNECTIONS
# ═══════════════════════════════════════════════════════

EXISTING_CONNECTIONS = """
  HYPERPOLY_v5.html          → Sandbox mode prototype
  BACKROOMS_MEP.html         → Layer infrastructure (MEP systems per color)
  MANIFESTATION_BRIDGE.html  → Layer transition logic (transfer functions)
  CINEMATOGRAPHY_ENGINE.html → Drone-Shaper camera system
  RESONANCE_HUD.html         → Dynamic soundtrack generator
  CHINESE_CALCULATOR.html    → 7-bead abacus = 7 layers
  
  checkpoint_system.py       → Maps 7 stages to all domains
  fractype_renderer_v2.py    → Layer transitions as vinculum
  procedural_video_learner.py → Veo-generated layer atmospheres
"""
