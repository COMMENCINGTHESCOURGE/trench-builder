# VINCULUM THEORY — Complete. Four Roles.
# May 16, 2026

THE_FOUR_ROLES = """
The vinculum is not ONE operator. It is FOUR.

  ROLE 1 — DIVISION BAR (fraction)
    a / b  = a divided by b
    Counterpart: no bar for multiplication (just xy, x·y, x×y)
    Our use: FracType. (numerator / denominator) = realized value.

  ROLE 2 — GROUPING SYMBOL
    a − b + c  (line over b+c means evaluate as one unit)
    Counterpart: parentheses () replaced this role
    Our use: Auto-close. {}[]() bind contents into one entity.

  ROLE 3 — MULTIPLIER (Roman numerals)
    V̄ = 5,000. The vinculum IS multiplication by 1,000.
    |V̄| = 500,000. Vertical sides multiply by 100,000.
    THE VINCULUM IS NOT THE COUNTERPART TO MULTIPLICATION.
    IT IS MULTIPLICATION ITSELF.
    Our use: Checkpoints. Each stage MULTIPLIES capability.

  ROLE 4 — SEQUENCE INDICATOR
    0.3̄ = 0.333... (infinite repeating)
    Counterpart: Π (capital Pi) = repeated multiplication
    Our use: Cron jobs. Every 4h × ∞ = infinite sequence.
"""

# ═══════════════════════════════════════════════════════
# THE VINCULUM NOW — All four roles in our system
# ═══════════════════════════════════════════════════════

ALL_FOUR_IN_OUR_SYSTEM = """
  DIVISION (Role 1):
    (pilot-intent / suit-resistance) = realized-movement
    (observed / ideal) = correction-factor
    (done / total) = progress

  GROUPING (Role 2):
    {code-block} = one executable unit
    [checkpoint-stage] = one milestone
    "once-in-a-lifetime" = one concept
    (numerator / denominator) = one fraction

  MULTIPLICATION (Role 3):
    The checkpoint AMPLIFIES capability.
    SCOOT × training = CRAWL
    CRAWL × practice = STAND
    STAND × balance = BOUNCE
    Each stage is a vinculum multiplying the previous stage.
    
    ROMAN VINCULUM IN OUR SYSTEM:
    V̄ = V × 1,000
    CHECKPOINT-5 = CHECKPOINT × 5× capability
    SUPINĒ = SUPINE × mastered = RUN

  SEQUENCE (Role 4):
    Cron every-4h̄ = runs forever (infinite sequence)
    Sievē = runs until stopped (repeating computation)
    Delta-learner̄ = improves every cycle (convergent sequence)
    Π(stages) = product of all checkpoint accomplishments
"""

# ═══════════════════════════════════════════════════════
# THE UNIVERSAL OPERATOR
# ═══════════════════════════════════════════════════════

UNIVERSAL = """
  The vinculum is the UNIVERSAL OPERATOR.
  
  It divides. It groups. It multiplies. It sequences.
  
  Same bar. Same glyph. Different meaning based on CONTEXT.
  
  ÷ / − over terms  = division
  − over expression  = grouping
  − over numeral     = multiplication
  − over decimal     = infinite repetition
  
  What changes is not the vinculum.
  What changes is WHAT IS BENEATH IT.
  
  Numbers beneath → division
  Expression beneath → grouping
  Roman numeral beneath → multiplication
  Decimal beneath → sequence
  
  The vinculum is the INTERPRETER.
  It means whatever the context demands.
  The vinculum doesn't change. The substrate does.
"""
