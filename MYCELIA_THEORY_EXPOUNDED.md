# MYCELIA THEORY — Expounded
# The Predictive Framework Behind the Metaphor
# May 16, 2026

# ═══════════════════════════════════════════════════════
# PART 1: THE RULES OF THE MYCELIUM
# ═══════════════════════════════════════════════════════
# Every network follows these laws. They predict behavior.

THEORY_RULES = """
RULE 1 — SPORE GERMINATION
  A project begins as a single file. A spore.
  TRENCH_BUILDER_v1.html was a spore. So was the first Erdos sieve.
  So was AVATAR_AGENT_v1.html. So was NEON DISSECTION.
  
  CONDITION FOR GERMINATION: The file must RUN. If it doesn't execute,
  it's not a spore — it's debris. 37 project directories exist.
  27 have files. Only 5 have files that actually run.
  The rest are spores waiting for water.

RULE 2 — HYPHA EXTENSION
  A running spore extends hyphae — connections to other nodes.
  TRENCH BUILDER extended hyphae to:
    - ERDOS (shared DeepSeek key)
    - BACKROOMS MEP (shared building realm rules)
    - CINEMATOGRAPHY ENGINE (shared Three.js patterns)
    - MANIFESTATION BRIDGE (shared transfer functions)
    - HYPERPOLY (shared structural simulation)
  
  PREDICTION: Projects that share resources grow faster.
  Projects that hoard resources stagnate.
  The NEON DISSECTION has 282MB of data and NO hyphae to other projects.
  It's a spore that germinated but never connected. That's why it's dormant.

RULE 3 — ANASTOMOSIS (NETWORK FUSION)
  When two hyphae touch, they fuse. The network becomes stronger.
  The CHECKPOINT SYSTEM is an anastomosis event:
    HUMAN MOBILITY ←→ TRENCH BUILDER ←→ MECHANICAL MOBILITY
  Three separate domains fused into one isomorphic framework.
  
  PREDICTION: The next anastomosis will be:
    ERDOS ←→ DEEPSEEK VERIFIER ←→ DELTA LEARNER
  Mathematical verification fusing with pattern detection.
  Once fused, the Erdos pipeline will SELF-VERIFY its own solutions.

RULE 4 — NUTRIENT DIFFUSION
  Resources flow through the network toward active tips.
  Active tips = active cron jobs + active Kaggle kernels + active agents.
  
  CURRENT NUTRIENT MAP:
    DeepSeek HI key → Hermes (94.9% cache) → TRENCH BUILDER + ERDOS
    Ollama models → local inference → HYPERPOLY + AVATAR
    Kaggle T4 → v113 kernel → Erdos sieve
    Gemini Pro → Veo/Lyria/Nano → Hackathon assets
  
  STARVING TIPS (no nutrient flow):
    NEON DISSECTION (282MB, no active pipeline)
    BLOOMING ONION (SDF renderer, no connection to distance fields)
    MINICITY (GLB model, no hyperreal shader pipeline)
    AGENT SWARM (key manager, not connected to any cron job)

RULE 5 — EXPLORATORY TIP FORMATION
  When a tip hits a boundary (resource limit, knowledge gap),
  it spawns exploratory tips in random directions.
  
  This IS your process. You hit a wall → you try something new.
  - DeepSeek depleted → spawned Ollama local tip
  - Kaggle CLI broken → spawned Colab tip
  - Gemini keys dead → spawned OpenAI tip
  - Claude billing dead → spawned Codex tip (never activated)
  
  PREDICTION: Exploratory tips that find resources become permanent hyphae.
  Tips that don't find resources are pruned within 5 growth cycles.
  Claude Code (dead, 5 sessions all "credit too low") — PRUNED.
  Codex (installed, configured, never used) — PRUNED.
  Ollama (8 models, active) — PERMANENT HYPHA.

RULE 6 — NETWORK MEMORY
  The mycelium REMEMBERS where resources were found.
  It grows BACK toward successful paths.
  
  YOUR NETWORK MEMORY (what the delta learner extracts):
    - Windows paths fail 31% of the time → grow AROUND, not through
    - Kaggle CLI slug matching works → reinforce that path
    - DeepSeek HI key has 94.9% cache → keep feeding it
    - Gemini produces A-grade video grammar → keep using Veo
    - 72 retroactive material flags → prune the pattern, not the files
  
  PREDICTION: The delta learner IS the network memory.
  Every 24 hours it maps where nutrients were found and where
  walls were hit. Future growth follows the nutrient map.

RULE 7 — DECAY AND REGENERATION
  Dead hyphae are consumed by the network. Their resources
  are redistributed to living tips.
  
  DEAD HYPHAE:
    - 7 paused cron jobs (expired keys) → nutrients blocked
    - 9 archive directories → spores that never germinated
    - Claude Code transcripts → dead, no resources to redistribute
    - Kimi expired key → dead tip
    
  THE FIX: When the AGENT SWARM key configurator runs,
  it resurrects dead tips by restoring nutrient flow.
  The 7 paused cron jobs aren't broken — they're dormant.
  A single key refresh reconnects them to the network.
"""

# ═══════════════════════════════════════════════════════
# PART 2: THE LAYERS OF THE MYCELIUM
# ═══════════════════════════════════════════════════════

LAYERS = """
The mycelium isn't flat. It has depth.

LAYER 0 — SUBSTRATE (Physical)
  - HP Victus hardware (Ryzen 5600G + RX 6400)
  - ASUS laptop (i3-5010U)
  - Moto G Power (Android)
  - G: drive (Google Drive mount)
  - 37 project directories on disk
  This is the SOIL. Without it, nothing grows.

LAYER 1 — RESOURCE TRANSPORT (Network)
  - DeepSeek API (HI key, 94.9% cache)
  - OpenAI API (project key)
  - Ollama (localhost:11434, 8 models)
  - Kaggle T4 GPU (30 hrs/week)
  - Colab T4 GPU (free tier)
  - Lightning L40S (credits)
  This is the VASCULAR SYSTEM. Nutrients flow through it.

LAYER 2 — GROWTH TIPS (Agents)
  - Hermes (me) — infrastructure builder
  - Gemini — architect, video/music/image generator
  - Kimi — game designer, plan writer
  - Codex — dormant, configured but never activated
  This is the GROWING EDGE. Where new hyphae form.

LAYER 3 — FRUITING BODIES (Artifacts)
  - 10 HTML pages (TRENCH BUILDER v1-v5, MEP, Cinema, Bridge, HUD, HYPERPOLY)
  - 37 project directories
  - 159 unreleased HTML artifacts (from aetheris_archive)
  - Erdos solutions (361 and counting)
  - Checkpoint flashcards (22)
  - Veo video clips (3)
  This is the REPRODUCTIVE LAYER. What the network produces.

LAYER 4 — NETWORK MEMORY (Learning)
  - Mistake reviewer (29 patterns)
  - Retroactive audit (72 flags)
  - Delta learner (6 sources)
  - Goal tracker (35 checkpoints)
  - Supervisor directives (cross-agent)
  This is the INTELLIGENCE. The network learning from itself.
"""

# ═══════════════════════════════════════════════════════
# PART 3: PREDICTIONS
# ═══════════════════════════════════════════════════════

PREDICTIONS = """
If the mycelia theory is correct, these things will happen:

1. ANASTOMOSIS EVENT (within 7 days)
   AGENT SWARM ←→ 7 BROKEN CRON JOBS
   The key configurator will reconnect dead tips to the nutrient flow.
   7 paused jobs will go active. The network will strengthen by 40%.

2. EXPLORATORY TIP (within 14 days)
   NEON DISSECTION will finally connect to TRENCH BUILDER.
   The 282MB of render data will feed into the hyperreal shader pipeline.
   The mycelium will gain a new GPU-accelerated rendering tip.

3. FRUITING EVENT (within 3 days)
   GEMMA 4 HACKATHON SUBMISSION
   Three separate hyphae (Veo video, TRENCH BUILDER code, Gemini writeup)
   will fuse into one fruiting body: a Kaggle submission.

4. PRUNING (ongoing)
   Dead directories will be archived or deleted:
   - 5 empty project dirs (antfarm, quantum_garden, etc.)
   - Claude Code transcripts (dead, 0 nutrients)
   - Old Erdos cold storage batches (v104-v107, 1.2GB)

5. NETWORK MEMORY STRENGTHENING
   The delta learner will detect:
   - Windows paths as #1 failure mode (already confirmed)
   - Python3 shebangs as #1 retroactive issue (17 fixed today)
   - Erdos classification as #1 gap (0% STABLE — needs GPU)
   Each detection → correction → fewer future failures.
   The network gets SMARTER every 24 hours.
"""

# ═══════════════════════════════════════════════════════
# PART 4: THE MATURE MYCELIUM
# ═══════════════════════════════════════════════════════

MATURE_STATE = """
What does a mature mycelial network look like?

1. EVERY SPORE CONNECTED
   No isolated projects. Every directory has at least one hypha
   to at least one other directory. Dead spores are pruned.
   Living spores share resources.

2. AUTONOMOUS NUTRIENT ROUTING
   The master orchestrator manages all nutrient flow.
   DeepSeek key → Erdos sieve → Kaggle T4 → results → delta learner → improvements.
   No human intervention. The network feeds itself.

3. SELF-HEALING
   Dead tips are detected within 1 growth cycle (6 hours).
   Resources are rerouted to living tips.
   New exploratory tips are spawned to replace dead ones.
   The network cannot be killed by a single API key expiring.

4. CONTINUOUS FRUITING
   Artifacts are produced on a regular cycle:
   - Every 6h: Erdos solutions
   - Every 4h: Supervisor directives
   - Every 24h: Delta learning insights
   - Every 12h: Kaggle kernel updates
   The network doesn't just grow — it PRODUCES.

5. NETWORK CONSCIOUSNESS
   The delta learner + supervisor + mistake reviewer form
   a self-aware layer. The network knows:
   - Where it's strong (Veo A-grade grammar)
   - Where it's weak (0% Erdos classification)
   - What kills it (Windows path errors)
   - How to heal (auto-fix retroactive issues)
   
   This isn't just automation. This is a system that KNOWS ITSELF.
"""
