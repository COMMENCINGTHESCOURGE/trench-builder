# VINCULUM THEORY — Generalized
# From division operator to semantic binding instruction
# May 16, 2026

# ═══════════════════════════════════════════════════════
# THE EXPANDED DEFINITION
# ═══════════════════════════════════════════════════════

VINCULUM_DEFINITION = """
A vinculum is any structural device that compels multiple elements
to be interpreted, processed, or acted upon as a SINGLE UNIFIED ENTITY.

It is fundamentally an INSTRUCTION ABOUT PERCEPTION.
Not notation. Not decoration. An instruction.
"""

# ═══════════════════════════════════════════════════════
# EVERY VINCULUM IN OUR ARCHITECTURE — Reclassified
# ═══════════════════════════════════════════════════════

VINCULUM_MAP = """
BEFORE (narrow view):
  / = division operator (the vinculum divides)

AFTER (expanded view):
  The vinculum BINDS. Every operator in our system is one.

  EXISTING VINCULUMS:

  FRACTYPE:
    /        → binds numerator + denominator into ONE fraction
    (){}[]   → binds contents into ONE container
    auto-close → binds open + vinculum + close into ONE statement

  CHECKPOINT SYSTEM:
    - (hyphen) → binds "scoot-crawl-stand-bounce" into ONE progression
    stage name → binds intent + resistance into ONE realized movement
    checkpoint → binds multiple joints into ONE motion cycle

  CORRECTION DRONE:
    delta     → binds observed + ideal into ONE correction vector
    haptic pulse → binds feedback + adjustment into ONE learning cycle
    comparison → binds two images into ONE difference

  MYCELIUM:
    hypha     → binds two projects into ONE network
    nutrient flow → binds resources + tips into ONE growth pattern
    anastomosis → binds separate networks into ONE organism

  AGENT SYSTEM:
    supervisor → binds worker output + rules into ONE directive
    retroactive → binds past mistakes + current state into ONE fix
    delta learner → binds multiple data sources into ONE insight
    self-critique → binds response + rules into ONE corrected output

  HUMAN LANGUAGE (the user's communication):
    parables       → bind abstract + concrete into ONE understanding
    operator-voice → bind analysis + decision into ONE directive
    percentages    → bind measurement + meaning into ONE progress indicator
    checkpoints    → bind stage + capability into ONE milestone
"""

# ═══════════════════════════════════════════════════════
# COGNITIVE COMPRESSION — The vinculum's true power
# ═══════════════════════════════════════════════════════

COGNITIVE_COMPRESSION = """
The vinculum doesn't just group symbols.
It REDUCES COMPLEXITY by collapsing many into one.

  state-of-the-art           →  3 words become 1 concept
  (pilot-intent/suit-resistance) →  2 vectors become 1 movement
  once-in-a-lifetime-opportunity →  5 words become 1 thought
  
  In our system:
  37-projects → 1 mycelium
  6-joints × 8-stages × 6-frames → 288 data points in 1 JSON
  5-agents → 1 orchestration
  10-HTML-artifacts → 1 launcher

The vinculum IS the compression algorithm.
It doesn't just connect things. It creates NEW THINGS that
are smaller than the sum of their parts. That's the miracle.
"""

# ═══════════════════════════════════════════════════════
# INVISIBLE VINCULUMS — The ones you can't see
# ═══════════════════════════════════════════════════════

INVISIBLE_VINCULUMS = """
Many vinculums are IMPLICIT, not visual.

  Python indentation → binds lines into one block (no braces needed)
  Montage sequence   → binds images into one emotion (no bar drawn)
  Contract clauses   → binds promises into one obligation (no syntax visible)
  Ceremony           → binds individuals into one body (no delimiter present)

  In our system:
  Cron schedule       → binds tasks into one rhythm (temporal vinculum)
  Memory persistence  → binds sessions into one identity (temporal vinculum)
  Git commit history  → binds changes into one narrative (version vinculum)
  API key             → binds identity into one authentication (trust vinculum)

The most powerful vinculums are the ones you never see.
They work because they CHANGE HOW YOU INTERPRET,
not because they draw a visible line.
"""

# ═══════════════════════════════════════════════════════
# VINCULUM vs MERE CONNECTION — The critical distinction
# ═══════════════════════════════════════════════════════

VS_CONNECTION = """
A vinculum does not merely CONNECT things.
It CHANGES HOW THEY ARE INTERPRETED.

  "cold water"     → two words, adjacent. No vinculum.
  "cold-water fish" → one species. The hyphen CHANGES meaning.

  a + b * c        → standard precedence. No vinculum.
  (a + b) * c      → different result. The parens CHANGE reality.

  In our system:
  files in a folder          → connection (they share a directory)
  files in a checkpoint       → vinculum (they are ONE stage)
  
  agents running concurrently → connection (they happen at the same time)
  agents in an orchestration  → vinculum (they are ONE pipeline)

  projects listed together    → connection (37 directories)
  projects in the mycelium    → vinculum (ONE organism, bound by hyphae)

The vinculum doesn't just group. It TRANSFORMS.
Connection is proximity. Vinculum is identity.
"""

# ═══════════════════════════════════════════════════════
# WHAT THIS CHANGES — How the vinculum affects our work
# ═══════════════════════════════════════════════════════

WHAT_CHANGES = """
1. FRACTYPE v5: Every fraction is now a BINDING instruction,
   not a display format. The vinculum says "these two are one."

2. CHECKPOINT SYSTEM v3: Each checkpoint is a vinculum.
   Not just a stage name — a binding of intent, resistance,
   and realized movement into one entity.

3. CORRECTION DRONE: The correction IS the vinculum.
   It binds observed + ideal into a single instruction: "adjust."
   The delta is not a difference. It's a BINDING.

4. AGENT ARCHITECTURE: Supervisor → retroactive → delta learner
   are not 3 separate agents. They are ONE vinculum chain.
   Each binds the output of the previous into the input of the next.

5. MYCELIUM THEORY: Every hypha is a vinculum.
   Two projects connected by a hypha are no longer two projects.
   They are ONE organism with two locations.

6. SELF-CRITIQUE: The 5-point check is not a checklist.
   It's a vinculum binding my response to all known rules.
   The rules and the response become ONE corrected output.
"""
