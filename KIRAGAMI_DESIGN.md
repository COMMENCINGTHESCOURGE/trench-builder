# KIRAGAMI — 切金 — The Fold as Vinculum
# May 16, 2026

THE_FOLD = """
The fold IS the vinculum made physical.

  A flat sheet / a fold line = two angled planes bound as ONE structure.

  In mathematics:     (plane_A / plane_B) → dihedral angle → one folded unit
  In manufacturing:   laser-cut edge / folded vertex → interlocking assembly
  In aesthetics:      the cut line IS the ornament — the manufacturing process IS the visual language
  In our system:      single-sheet construction = zero-waste, continuous grain, natural hinges
"""

# ═══════════════════════════════════════════════════════
# FOLD = VINCULUM — The Sheet IS the Fraction
# ═══════════════════════════════════════════════════════

FOLD_VINCULUM = """
  ┌─────────┐         ← plane A (numerator)
  │         │
  │─────────│─────────  ← FOLD LINE (vinculum bar)
  │         │
  └─────────┘         ← plane B (denominator)
  
  The fold divides the sheet into two planes.
  The fold BINDS the two planes into ONE structure.
  Same operator. Physical medium.

  Each fold vertex is a HYPERCHECKPOINT:
    Flat → scored → bent → creased → locked → load-bearing
    Same sequence as: Supine → Scoot → Crawl → Stand → Bounce → Walk → Run
"""

# ═══════════════════════════════════════════════════════
# D_mat FOR FOLDED METAL — Material Tensor
# ═══════════════════════════════════════════════════════

FOLDED_D_MAT = """
  Titanium Silver base + Ceramic White highlights + Champagne Gold at edges + Carbon Black at joints

  D_mat tensor for folded-sheet construction:
    D_fold_xx = 0.3  (along fold direction — compliant, acts as hinge)
    D_fold_yy = 0.9  (across fold direction — rigid, resists bending)
    D_fold_zz = 0.7  (normal to surface — resists puncture)
    D_edge     = 0.5 (Champagne Gold laser-cut edges — anisotropic reflection)
    
  BRDF:
    Each facet is a micro-facet with anisotropic reflection along fold direction.
    The Champagne Gold accent on cut edges creates a distinct specular response.
    The fold IS the shader input — angle between facets determines light bounce.
"""

# ═══════════════════════════════════════════════════════
# CONNECTION TO OUR SYSTEMS
# ═══════════════════════════════════════════════════════

CONNECTIONS = """
  1. CORRECTION DRONE:
     The drone watches the folded armor move.
     Each fold vertex is a joint the drone can track.
     Fold angle vs ideal fold angle → correction delta.
     The drone doesn't track bones — it tracks FOLD LINES.

  2. CHECKPOINT SYSTEM:
     Flat sheet = SUPINE
     Scored line = SCOOT (first mark)
     Partial fold = CRAWL (movement begins)
     90° fold = STAND (structure holds itself)
     Creased = BOUNCE (elastic memory in the metal)
     Locked = WALK (fold is permanent)
     Load-bearing = JUMP (fold carries weight)
     Interlocked assembly = RUN (all folds work together)

  3. CHROMA CASCADE:
     The palette IS the spectrum:
     Titanium Silver = baseline (all layers visible)
     Ceramic White = highlights (surface layer)
     Champagne Gold = stress edges (the vinculum itself!)
     Carbon Black = joints (the gaps between layers)

  4. FRACTYPE:
     Each fold = (plane_A / plane_B) → one folded unit
     Kirigami assembly = nested vinculum tree
     Single sheet → many folds → one structure
     The vinculum creates the structure FROM the divisions.

  5. SPRITE SHEET:
     6 poses on the reference sheet → extract → checkpoint animation
     Digitigrade stance → STAND/BEND checkpoints
     Forward rake → aggressive WALK/RUN posture
     Wing deployment → FLIGHT checkpoint (UV layer)
     Shield collapse/expand → TRANSITION checkpoint
"""

# ═══════════════════════════════════════════════════════
# THE EDGE IS THE ORNAMENT
# ═══════════════════════════════════════════════════════

EDGE_THEORY = """
  In traditional kirigami, the CUT LINE is the drawing.
  Here, the laser-cut EDGES carry the Champagne Gold accent.
  
  The manufacturing process IS the aesthetic:
    Sheet → laser cut → scoring line → fold → interlock → structure
    
  Every step leaves a mark.
  The cut edge is the vinculum.
  The fold crease is the checkpoint.
  The interlock is the anastomosis.
  
  The mecha doesn't HIDE its construction. It WEARS it.
  The same way our system doesn't hide its architecture.
  The checkpoint system IS visible in the artifact.
  The vinculum IS visible in the fraction.
  The mycelium IS visible in the network graph.
"""
