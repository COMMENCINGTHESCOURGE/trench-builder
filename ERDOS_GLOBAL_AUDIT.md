# ERDOS GLOBAL AUDIT — May 17, 2026
# 10 projects × colour badges × verb gates × cascade targets

SPECTRUM = """
═══ PROJECT SPECTRUM ═══

  INFRARED (0):  dev_archive       522 files  ×5→IR  BREACH (self-destructive loop)
  RED (1):       erdos-straus       46 files  ×5→BL   OK     EXTRUDE(N) BEVEL(B) WELD(N) PAINT(S)
  ORANGE (2):    part_affordance     2 files  ×5→RD   OK     EXTRUDE(S) BEVEL(B) WELD(S) PAINT(N)
  YELLOW (3):    victus_bios_tool    3 files  ×5→IN  BREACH  ALL VERBS BREACH
  GREEN (4):     trench_builder    103 files  ×5→OR   OK     EXTRUDE(N) BEVEL(B) WELD(N) PAINT(S)
                 blooming-onion     13 files  ×5→OR   OK     same verb gates as trench
  BLUE (5):      kaggle_submissions  5 files  ×5→VI   OK     EXTRUDE(S) BEVEL(B) WELD(S) PAINT(N)
  INDIGO (6):    ale                15 files  ×5→YL  BREACH  ALL VERBS BREACH
                 lenticular_pipeline  6 files  ×5→YL  BREACH  ALL VERBS BREACH
  VIOLET (7):    (empty)
  ULTRAVIOLET(8): neon_dissection   17 files  ×5→GR   OK     EXTRUDE(S) BEVEL(B) WELD(S) PAINT(N)

═══ VERB GATE SUMMARY ═══

  BEVEL (×3):  BREACH for ALL projects. Never use unless critical.
  EXTRUDE (×2): SAFE on OR, BL, UV. NEUTRAL on RD, GR. BREACH on IR, YL, IN.
  WELD (×5):   SAFE on OR, BL, UV. NEUTRAL on RD, GR. BREACH on IR, YL, IN.
  PAINT (×7):  SAFE on RD, GR. NEUTRAL on OR, BL, UV. BREACH on IR, YL, IN.

═══ COMPRESSION ═══

  732 files / 8 unique layers = 92× apparent redundancy
  VIOLET (7) is EMPTY — the gap between INDIGO and UV
  VIOLET = time dilation = checkpoint JUMP = the missing project
"""

ACTION = """
  FIXES:
    1. dev_archive:  drop 3 files → mod9=6 (INDIGO) → cascade to YL
       Better: drop 4 files → mod9=5 (BLUE) → cascade to VIOLET
       
    2. ale + lenticular:  add 1 file each → mod9=7 (VIOLET) → fills the gap
       VIOLET (7) ×5 → mod9=8 (UV) = STABLE cascade
    
    3. victus_bios_tool:  add 4 files → mod9=7 (VIOLET) → stable
    
    4. PART_AFFORDANCE (OR, 2 files):  already perfect
       OR (2) ×5 → RD (1) = STABLE cascade
       Safe to EXTRUDE and WELD. Never BEVEL.
"""
